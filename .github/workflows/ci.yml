name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: IAmNo1Special/actions/.github/workflows/python-ci.yml@v1
    with:
      python-version: "3.13"
      lint-commands: |
        uv run ruff check .
        uv run ruff format --check .
